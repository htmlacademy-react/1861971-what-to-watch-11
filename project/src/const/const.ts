export enum AppRoute {
  Root = '/',
  Login = '/login',
  List = '/mylist',
  Films = '/films/',
  Review = '/review',
  View = '/player/',
  Mistake = '*',
}

export enum AuthorizationStatus {
  Auth = 'AUTH',
  NoAuth = 'NO_AUTH',
}
